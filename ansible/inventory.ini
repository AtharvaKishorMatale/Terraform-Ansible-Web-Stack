# ansible/inventory.ini
# This file serves as a placeholder.
# It will be dynamically populated by the GitHub Actions workflow with the actual EC2 Public IP address.

[web_servers] 
# This defines an Ansible group named 'web_servers'.
# Our playbook will target hosts within this group.
# Example of how the line will look after GitHub Actions populates it:
# web_server_host ansible_host=YOUR_EC2_PUBLIC_IP
