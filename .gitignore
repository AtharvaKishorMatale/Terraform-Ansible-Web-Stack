# Terraform ignores
.terraform/          # Directory where Terraform stores plugins and modules
*.tfplan             # Binary plan files generated by 'terraform plan'
*.tfstate            # The Terraform state file (critical, never commit to public repo)
*.tfstate.backup     # Backup state files
.terraform.lock.hcl  # Lock file for Terraform providers

# Ansible ignores
.ansible/            # Directory where Ansible might store temporary files or caches

# SSH key (local private key) - CRITICAL: NEVER commit your private SSH key!
.ssh/                # Ignores the entire .ssh directory if it were ever in the project root
